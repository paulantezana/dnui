extends ../../config/template
append config
    -title = "Spin"
    -description = "Spin."
    -ogUrl = "/index.html"
    -template = 2

block main
    .pd-grid.col-gap.row-gap.l-grid-2
        +codeBox2(
            'Spin',
            'Mixin',
            [
                {
                    languaje: 'scss',
                    code: '@include pd-loading_template(1em);'
                }
            ]
        )
            p @include pd-loading_template(1em);
